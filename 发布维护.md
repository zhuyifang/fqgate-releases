# FQGate 自动发布维护

这个仓库是 FQGate 正式发行的唯一所有者。私有源码仓库只能创建或更新 Draft Release 并发送 `fqgate-draft-ready` 通知；是否公开、版本清单内容和稳定通道都由本仓库的“正式发布 FQGate”工作流决定。

## 一次性设置

1. 在 Settings → Actions → General 中把 Workflow permissions 设为 `Read and write permissions`。
2. 创建 Environment `fqgate-production`，设置至少一名 required reviewer。不要在这里保存私有源码仓库的 Token 或源码。
3. 确保 `main` 的分支规则允许该发布工作流提交 `releases/<版本>.json`、`releases/stable.json` 和 README。若组织策略要求所有修改都经过 PR，应先调整发布治理方案；否则 Release 公开后稳定清单可能无法完成切换。
4. 保留 Actions 中的仓库级 `GITHUB_TOKEN` 默认来源，不创建自建 GitHub App，也不在本仓库保存跨仓库 PAT。

跨仓库 Token 应在私有 `fqgate` 仓库中创建为 Environment secret `FQGATE_RELEASES_TOKEN`。它必须是仅授权 `zhuyifang/fqgate-releases`、仅具有 `Contents: Read and write` 的 fine-grained personal access token。

## 发布顺序

1. 私有仓库完成 Windows x64、macOS arm64 和 macOS x86_64 构建，把安装包、SHA-256、平台元数据和发布请求上传到同一 Draft Release。
2. 本仓库收到 `repository_dispatch`，在审批前执行只读校验。
3. 审批 `fqgate-production` 后，工作流再次校验 Draft Release，先写入未发布版本清单，再公开 GitHub Release。
4. GitHub 返回真实发布时间后，工作流最后写入已发布版本清单、稳定版清单和 README 下载段落。

稳定版清单是最后一步。任何平台缺失、文件名含 `TEST` 或 `DEBUG`、文件大小或 SHA-256 不符、校验文件不匹配、API 摘要不一致时，发布都会停止。

## 故障恢复

- Draft Release 已存在但通知失败：在本仓库手工运行“正式发布 FQGate”，填写同一版本。
- 审批前失败：修复 Draft 资产后重跑；脚本会重新完整校验。
- Release 已公开但稳定清单提交失败：不要删除或覆盖已公开资产。修复主分支写权限后，用同一版本重跑；工作流会验证公开资产并继续切换稳定通道。
- 版本已经低于当前 `releases/stable.json`：恢复流程会拒绝降级，需先确认输入版本是否错误。

日常发布不需要手工编辑清单，也不需要在两台构建机之间复制文件。Gitee 镜像若继续保留，仍需使用独立的同步和验收流程，不能把镜像成功当作 GitHub 稳定通道已经切换。
